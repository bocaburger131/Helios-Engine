# RuFlo V3 Runtime Configuration
# Generated: 2026-05-16T09:07:54.262Z
# Keep in sync with ../claude-flow.config.json (agents.providers + llm models).
# Do not delete the JSON file — CLI `config get agents.providers` reads it.

version: "3.0.0"

# Primary LLM provider for swarm/agents (API key via GOOGLE_API_KEY or GEMINI_API_KEY env)
defaultProvider: google

agents:
  providers:
    - name: google
      model: gemini-2.5-pro
      priority: 1
      enabled: true
    - name: anthropic
      model: claude-opus-4-7
      priority: 2
      enabled: false

llm:
  defaultProvider: google
  coordination:
    model: gemini-2.5-pro
  routing:
    model: gemini-2.5-flash

swarm:
  topology: hierarchical-mesh
  maxAgents: 15
  autoScale: true
  coordinationStrategy: consensus

memory:
  backend: hybrid
  enableHNSW: true
  persistPath: .claude-flow/data
  cacheSize: 100
  # ADR-049: Self-Learning Memory
  learningBridge:
    enabled: true
    sonaMode: balanced
    confidenceDecayRate: 0.005
    accessBoostAmount: 0.03
    consolidationThreshold: 10
  memoryGraph:
    enabled: true
    pageRankDamping: 0.85
    maxNodes: 5000
    similarityThreshold: 0.8
  agentScopes:
    enabled: true
    defaultScope: project

neural:
  enabled: true
  modelPath: .claude-flow/neural

hooks:
  enabled: true
  autoExecute: true

mcp:
  autoStart: false
  port: 3000
